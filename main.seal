include './lexer.seal'
include './parser.seal'
include './ast.seal'
include './compiler.seal'

const
  file = fopen('test.seal', 'r')
  src = fread(file)
  lexer = Lexer(src)

get_tokens(lexer)

fclose(file)

const root = parse(lexer.tokens)
//print_ast(root)
compile(root)
writeln(sout)
