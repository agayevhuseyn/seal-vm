include './lexer.seal'
include './parser.seal'
include './ast.seal'
include './compiler.seal'

const
  file = fopen('test.seal', 'r')
  src = fread(file)
  tokens = get_tokens(src)

fclose(file)

const root = parse(tokens)
//print_ast(root)
compile(root)
writeln(sout)
